@page
@model MediatorBuddy.Samples.Razor.Pages.Widgets.IndexModel
@{
}

<ul>
@foreach (var widget in Model.ViewModel.Widgets)
{
        <li>
            @widget.Id - @widget.Name <button><a asp-page="Details" asp-route-id="@widget.Id">Details</a></button>
        </li>
}
</ul>